 {{#each products as |product|}}
 <div class="flex items-stretch bg-rose-600 rounded-xl text-center border-4 border-lime-800">
  <div class="bg-black text-rose-600 w-max">
    <h1 class="">{{product.id}} {{product.name}}</h1>
  </div>
      
      <img class=" w-40 h-40 self-center" src="{{product.imageURL}}" alt="{{product.category}}">
      <h3 class="w-full">Price:{{product.price}}</h3>
    </div>
{{/each}}
<div class="menu-container">


<script src="/js/add-dish.js"></script>
