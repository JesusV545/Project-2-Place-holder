
<main>

<div class="bg-rose-600 rounded-xl text-center border-4 border-lime-800">
  <div>
    <h1 class="">{{product.id}} {{name}}</h1>
    
     <div class="mb-2"> 
              <a href="javascript:void(0);" class="btn btn-success" onclick="addToCart('{{product.id}}', '{{name}}', '{{imageURL}}', {{price}})">Add to cart</a>
  </div>

  <img class=" w-40 h-40 self-center m-auto" src="{{imageURL}}" alt="{{category}}">
  {{!-- <h2>Type: {{ category }}
  </h2> --}}
  <h3 class="w-full text-xl">Price: ${{price}}</h3>
</div>
<script src="/js/cart.js"></script>
<script src="/js/cart.js"></script>

</main>